export enum btnViewType{
    button,
    arrow
}

export enum btnBorderSizeType{
    xl,
    l,
    m,
    arrow
}

export enum btnColorType{
    white,
    blue,
    red
}

export enum btnArrowType{
    up,
    right,
    down,
    left
}