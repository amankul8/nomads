import { MapContent } from "@/components/content/map";
import {Layout} from "@/layout"

export default function Map() {

  return (
    <Layout>
        <MapContent/>
    </Layout>
  );
};
